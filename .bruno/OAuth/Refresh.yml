info:
  name: Refresh
  type: http
  seq: 2

http:
  method: GET
  url: "{{xrpc}}.oauth.refresh"
  auth:
    type: bearer
    token: "{{refreshToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
