info:
  name: OAuth Metadata
  type: http
  seq: 1

http:
  method: GET
  url: "{{origin}}/.well-known/atproto-oauth-meta.json"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
