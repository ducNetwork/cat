info:
  name: Get Profiles
  type: http
  seq: 1

http:
  method: GET
  url: "{{xrpc}}.users.getProfiles?ids={{handle}}"
  params:
    - name: ids
      value: "{{handle}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
