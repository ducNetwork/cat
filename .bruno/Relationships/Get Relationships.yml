info:
  name: Get Relationships
  type: http
  seq: 1

http:
  method: GET
  url: "{{xrpc}}.relationships.getRelationships"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
