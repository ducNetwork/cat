info:
  name: User
  type: http
  seq: 1

http:
  method: GET
  url: "{{xrpc}}.users.getUser?id={{handle}}"
  params:
    - name: id
      value: "{{handle}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
