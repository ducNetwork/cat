info:
  name: OAuth Metadata
  type: http
  seq: 1

http:
  method: GET
  url: "{{origin}}/.well-known/oauth-client.json"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
