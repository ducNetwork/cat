info:
  name: Update Profile
  type: http
  seq: 2

http:
  method: POST
  url: "{{xrpc}}.users.updateProfile"
  body:
    type: json
    data: |-
      {
        "displayName": "netchimken"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
